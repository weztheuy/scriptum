// public/js/pages/index.js
(function () {
  // Пример заглушки — здесь можно делать fetch('/api/works?sort=new') и рендерить первые 3 карточки.
  // Для учебного варианта оставляем статические карточки, уже в HTML.
  // Если нужно — реализую renderWorkCard и подгрузку.
})();
